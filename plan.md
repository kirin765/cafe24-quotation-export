# B2B 견적 내보내기 — 구현 계획

- 작성일: 2026-09-16
- 프로젝트: `cafe24-quotation-export`
- 상태: A 완료(2026-09-16). C 진행 중 — 설치/OAuth·토큰 저장·상품 조회까지 구현, 초안 저장·버전은 미구현. B·D 미착수
- 담당: Giwan(제품 선택·실몰 수동 검증), 개발 세션(구현·자동 검증)
- 첫 산출물: 상품 목록을 편집 가능한 XLSX와 전달용 PDF로 만드는 로컬 데모.

## 1. 목적과 근거

**Cafe24 B2B 판매자가 상품·수량·협의 단가로 견적을 작성하고 거래처에 Excel/PDF 파일로 전달한다.** 구매사 내부 결재에 필요한 파일을 만들기 위해 상품 정보를 다시 입력하는 수고를 줄일 수 있는지 시험한다. 반복 빈도·절감 시간·지불 의사는 미검증이다.

- [견적 발주 게시판](https://store.cafe24.com/kr/apps/2365)에 Excel 다운로드 요청과 2022-10-06 당시 미지원 답변이 있다. 오래된 요청이며 현재 경쟁 기능 부재를 입증하지 않는다.
- 이전 연구에서 [Cafe24 견적서 관리](https://support.cafe24.com/hc/ko/articles/7738699610137)와 [견적서 FAQ](https://support.cafe24.com/hc/ko/articles/18535543331993)를 확인했다. 기본 견적 출력이 이미 있으므로 단순 인쇄가 차별점은 아니다.
- 선택 이유 가설: **편집 가능한 품목표 + 운영자가 승인한 협의 단가 + 같은 내용의 PDF**.
- 경쟁 존재는 후보 탈락 사유가 아니다. 편의성·가격 우위는 별도 시험해야 한다.
- [후보 비교](../llm-wiki/reports/cafe24-broad-experiment-candidates-2026-09-16.md), [연구 원자료](../llm-wiki/raw/cafe24-broad-candidates-2026-09-16/storefront-notes.md).

## 2. 범위

### 첫 데모 — 총 3시간, 추가 현금 0원 제안

1. 합성 상품 10개 또는 정해진 CSV 양식을 불러온다.
2. 상품명·옵션·수량·협의 단가를 수정한다.
3. 수신 회사명, 유효기한, 납기·배송 조건, 비고를 입력한다.
4. 견적 미리보기에서 금액을 확인한다.
5. 같은 데이터로 XLSX를 다운로드하고 PDF로 저장한다.

서버·DB·OAuth 없이 합성 데이터로 시작한다. PDF는 우선 인쇄 전용 화면과 브라우저의 ‘PDF로 저장’을 사용한다. 다운로드 버튼 하나로 PDF 파일을 생성하는 기능과 구분해 안내한다.

### 첫 연동 MVP

- Cafe24 상품·옵션 조회로 품목 추가 시간을 줄인다.
- 운영자가 단가를 확정하고 견적 초안을 저장한다.
- 견적 번호와 버전을 가진 사본으로 XLSX/PDF를 내보낸다.
- 이전 견적을 복제해 새 견적을 만든다.
- 파일 전달은 기존 메일·상담 채널에서 운영자가 직접 한다.

### 제외

- Cafe24 기본 견적 데이터 자동 가져오기: 지원 API와 권한 미확인.
- 견적 협상 게시판, 전자서명, 자동 이메일/SMS, CRM.
- 장바구니·개인 결제창·주문 자동 생성, 재고 예약.
- 세금계산서·회계 장부 발행, 법정 문서 적합성 보증.
- 쿠폰·회원가·배송비까지 실제 결제창과 자동 일치시키기.
- 다국어·외화·모든 거래처 양식. 첫 버전은 한국어·KRW·고정 양식 하나.

견적은 운영자가 작성한 주문 전 제안이다. 결제 완료나 주문 생성으로 표시하지 않는다.

## 3. 화면과 사용 흐름

| 화면 | 기능 | 완료 조건 |
|---|---|---|
| `/demo` | 합성 데이터, CSV 입력, 초기화 | 계정 없이 반복 재현; 실제 정보 입력을 요구하지 않음 |
| `/quotes` | 저장 목록·검색·복제 | 몰별 문서가 분리되고 빈 상태·실패 상태가 구분됨 |
| `/quotes/new` | 수신처·품목·수량·단가·조건 입력 | 필수값과 오류를 행별로 표시 |
| `/quotes/[id]` | 초안 편집·확정·새 버전 만들기 | 확정 버전은 직접 덮어쓰지 않음 |
| `/quotes/[id]/print` | A4 인쇄용 미리보기 | 한글·긴 품목명·페이지 분할·합계 표시 확인 |
| `/settings` | 공급자 표시 정보·로고·기본 문구 | 변경해도 이미 확정한 견적에는 소급 반영되지 않음 |

데모는 한 편집 화면과 인쇄 화면만 구현해도 된다. Cafe24 연동 전에는 상품 입력을 CSV와 수동 입력으로 제한한다.

## 4. 입력·금액 규칙

### 데모 CSV

```csv
item_code,product_name,option_name,quantity,unit_price
SAMPLE-001,샘플 타월,화이트,10,5000
SAMPLE-002,샘플 컵,300ml,20,3000
```

- 필수: 상품명, 양의 정수 수량, 0 이상의 정수 원 단위 단가.
- 품목 코드·옵션명은 선택. 코드가 같아도 다른 조건의 행일 수 있으므로 자동 병합하지 않는다.
- 초기 제한: CSV 1MB, 200행. UTF-8/BOM과 인용부호·쉼표·개행을 처리하는 파서 사용.
- 헤더 누락·행별 오류를 보여주고, 조용히 일부 행만 가져오지 않는다.
- 코드의 앞자리 0은 문자열로 보존한다.

### 초기 계산 모델

- `행 금액 = 수량 × 운영자 입력 단가`.
- `상품 합계 = 행 금액 합계`.
- `견적 총액 = 상품 합계 + 운영자가 별도로 입력한 조정 금액`.
- 조정 금액에는 필수 설명을 붙인다. 예: 배송비 또는 일괄 할인. 할인 후 총액은 음수가 될 수 없다.
- 자동 부가세 계산은 첫 데모에서 제외한다. 세금 포함 여부 등 가격 조건을 운영자가 명시하도록 하고, 세금계산서로 오해하게 하는 표현을 피한다.
- 숫자는 원 단위 정수로 계산하고 안전한 범위를 검증한다. 부동소수점이나 화면의 형식화된 문자열을 계산 원본으로 사용하지 않는다.

세금 분리·면세·혼합 과세가 실제 필수라면 별도 범위로 계산 규칙·반올림·검증 사례를 확정한 뒤 구현한다. 임의의 세율을 전체 품목에 자동 적용하지 않는다.

### 일치성

- 화면·XLSX·PDF는 동일한 `QuoteDocument`와 계산 함수를 사용한다.
- XLSX에는 견적 시점의 금액을 숫자 값으로 내보낸다. 첫 버전은 자동 재계산 수식을 제공하지 않는다.
- Excel 파일은 편집 가능하지만 수량/단가 수정 후 합계가 자동 재계산되지 않는다는 점을 파일 안내에 표시한다. 재계산이 필요하면 앱에서 수정 후 다시 내보낸다.
- 사용자가 재계산 수식을 필요로 하는지 파일럿에서 확인한다. 수식을 추가할 때는 지원 스프레드시트에서 재계산 동작을 검증한다.
- 앱 밖에서 수정한 Excel과 기존 PDF는 자동 동기화되지 않는다. 문서 번호·버전·작성시각을 두 파일에 표시한다.

## 5. 구현 구조 — 제안

- TypeScript + Next.js. 데모는 브라우저에서 CSV 파싱·편집·XLSX 생성.
- XLSX 라이브러리는 구현 시작 시 라이선스·유지 상태·브라우저 지원을 확인한 뒤 한 개를 선택하고 버전을 고정한다.
- PDF는 print CSS부터 시작. 직접 PDF 다운로드가 필요해지면 한글 폰트 포함·페이지 분할·운영비를 검증한 renderer를 선택한다.
- 연동 MVP는 Postgres에 초안/확정 버전을 저장하고 서버에서 tenant 권한을 확인한다.
- 기존 Cafe24 앱에서 OAuth 처리 패턴은 참고하되 토큰·앱 ID·비밀값·브랜딩은 복사하지 않는다.
- mise 프로젝트 설정과 하나의 패키지 관리자/lockfile을 사용한다.

```text
src/app/                     편집·목록·인쇄 화면, HTTP 경계
src/features/quotes/model.ts  QuoteDocument, 검증·계산
src/features/quotes/csv.ts    입력 파싱·행별 오류
src/features/quotes/xlsx.ts   문서 → XLSX
src/features/quotes/print/    문서 → 인쇄 레이아웃
src/lib/cafe24/               launch/OAuth/상품 adapter
src/lib/auth/                 운영자 세션·tenant 격리
src/fixtures/                합성 상품·견적
db/migrations/               연동 단계의 앱 전용 스키마
QA.md                        구현 시작 때 작성할 수동 검증표
```

핵심 계산·문서 생성은 UI와 Cafe24 API에서 분리한다. 그래야 CSV 데모를 그대로 상품 연동 버전으로 확장할 수 있다.

## 6. 데이터 모델과 문서 버전

| 엔터티 | 주요 필드 / 제약 |
|---|---|
| `shops` | tenant ID, mall ID, shop number, 설치 상태 |
| `oauth_credentials` | 앱 전용 암호화 토큰, 만료시각, 검증된 설치 범위 |
| `quote_drafts` | tenant, 수신 회사명, 공급자 정보, 날짜·조건·편집 revision |
| `quote_items` | 초안, 행 순서, 상품/옵션 코드, 이름, 수량, 단가 |
| `quote_versions` | tenant, 견적 번호, 버전, 확정 문서 JSON, 계산 합계, 확정시각 |
| `audit_events` | tenant, 문서 ID, 확정·삭제·내보내기 이벤트; 본문/수신처는 로그 제외 |

- 상품 카탈로그를 바꿔도 확정 문서는 변하지 않도록 이름·단가·공급자 정보를 snapshot으로 저장한다.
- 확정 버전 수정은 새 초안과 새 버전을 만든다. 견적 번호/버전은 tenant 안에서 유일하다.
- 두 탭에서 편집하면 revision으로 충돌을 감지한다. 늦은 저장이 새 내용을 덮어쓰지 않게 한다.
- MVP에서는 파일을 영구 저장하기보다 확정 snapshot에서 요청 시 생성한다. 파일 생성 실패가 문서 내용을 바꾸지 않게 한다.
- 고객 연락처·주소는 초기 기능에 필요하지 않으므로 수집하지 않는다. 수신처 회사명도 외부 공개하지 않는다.

## 7. Cafe24 연동 전 검증

다음은 확인 과제이며 현재 가능한 API로 확정한 것이 아니다. 구현 시 [공식 Admin API](https://developers.cafe24.com/docs/en/api/admin/)와 개발자 문서를 확인한다.

- [ ] 상품/옵션 조회 endpoint, 최소 scope, pagination·호출 제한·멀티몰 구분 확인.
- [x] 상품 판매가가 협의 단가 또는 옵션별 최종 결제 가격과 다를 수 있음을 UI에 반영.
- [x] 카탈로그의 초기 단가를 제안값으로만 사용하고 운영자 확인 후 확정.
- [ ] 기본 견적 가져오기 API 존재·접근 범위는 별도 확인. 없으면 수동 입력/CSV 유지.
- [x] launch HMAC/timestamp, 전체 query 보존, OAuth state, 운영자 세션 구현.
- [ ] 앱 전용 토큰 저장·timezone·동시 refresh·일시 API 오류 처리.
- [x] 사용 API 버전 고정, scope/권한 철회 시 접근 차단.
- [x] 앱 삭제 시 보관·내보내기·삭제 정책과 실제 동작 일치.

스토어프론트 스크립트 삽입 없이 운영자용 앱부터 시작한다. 주문 쓰기 권한은 이 범위에 필요하지 않다.

**C 단계 진행 기록(2026-09-16).** 앱을 개발자센터에 등록하고(Web application / Authorization Code, Redirect URI `/api/auth/callback`, 운영자 권한은 상품 읽기 + 앱, 고객 권한 없음) 설치 흐름을 구현했다.

- 구현: `/api/auth/install`(launch 검증 → OAuth), `/api/auth/callback`(코드 교환·세션), `/api/webhooks/cafe24`(90077/90078 → 토큰 삭제), `/api/cafe24/products`·`/api/cafe24/products/{no}/options`, `src/lib/{cafe24,launch,store,token}.ts`, `/demo`의 상품 선택기.
- 문서로 확인한 사실: 상품 목록은 `GET /admin/products`, scope는 `mall.read_product`, `limit` 최대 100·`offset` 최대 5000, 호출 40/초(버킷), 공식 쿼터 3,000회/10분. 옵션은 `GET /admin/products/{product_no}/options`. API 버전은 릴리스 후 1년 유효(2026-09-01은 2027-09-01까지). 웹훅은 서명이 아니라 `X-API-Key` 고정 인증정보로 검증하며 payload는 `{ event_no, resource: { mall_id, ... } }`이다.
- 남은 항목: 멀티쇼핑몰(`shop_no`) 구분이 실제로 필요한지 테스트몰에서 확인. 토큰 동시 refresh 잠금은 아직 없다(같은 몰에서 동시 요청이 겹치면 refresh가 중복될 수 있음).
- 검증: 단위·stub 테스트 67개, 프로덕션 빌드 HTTP 라우트 확인 20개, 미설치 브라우저 동작 확인. 실몰 설치·상품 조회는 **미검증**(자동 검증은 스텁)이라 테스트몰 설치가 다음 관문이다. 설치 한도(기본 5회)를 쓰므로 순서를 정해 한 번에 통과시켜야 한다.
- 필요한 운영 설정: `CAFE24_CLIENT_ID`·`CAFE24_CLIENT_SECRET`·`CAFE24_REDIRECT_URI`·`CAFE24_WEBHOOK_API_KEY`·`DATABASE_URL`. 토큰 저장은 Neon 등 Postgres 무료 티어(추가 현금 0원)로 시작한다.

## 8. 문서 출력과 접근 보호

- 문자열 셀을 명시적으로 문자열로 작성하여 상품명/메모의 `=`, `+`, `-`, `@` 등이 실행 가능한 Excel 수식으로 해석되지 않게 한다.
- HTML 인쇄 화면은 입력 문자열을 escape하고 임의 HTML을 허용하지 않는다.
- 공급자 로고는 초기에는 합성 이미지. 실서비스 업로드를 추가할 때 형식·크기 제한과 이미지 재인코딩을 적용한다. 임의 외부 URL을 서버에서 가져오지 않는다.
- 모든 조회·수정·내보내기는 인증된 tenant로 제한한다. URL의 quote ID만으로 문서를 내려받을 수 없게 한다.
- 민감한 문서 응답의 공유 캐시를 방지하고 다운로드 파일명을 정규화한다.
- 첫 버전은 공개 공유 URL을 만들지 않는다. 필요가 확인되면 만료·회수 가능한 별도 기능으로 설계한다.
- 삭제·앱 제거 시 문서/임시 출력 파일을 정리하는 정책을 출시 전에 정한다. 백업 보관 기간도 안내와 일치시킨다.

## 9. 단계별 작업

### A. 로컬 데모 — 3시간 제안

- [x] 30분: scaffold, QuoteDocument, 샘플 CSV·계산/검증 함수.
- [x] 60분: CSV 가져오기, 행 편집, 조건 입력, 합계 미리보기.
- [x] 45분: XLSX 내보내기, 문서 번호·조건·숫자 형식.
- [x] 30분: A4 인쇄 CSS, 브라우저 PDF 저장 안내.
- [x] 15분: 샘플 금액 대조, 한글/긴 행 확인, 남은 작업 기록.

**완료 조건:** 합성 품목 10개를 편집한 뒤 XLSX와 PDF를 만들 수 있고, 두 파일의 품목·수량·단가·합계·버전이 일치한다. 3시간 초과 시 자동 연장하지 않고 결과와 부족한 부분을 기록한다.

**구현 결과(2026-09-16):** `src/features/quotes` 아래 model·csv·xlsx·print를 분리해 구현했다. 검증은 `QA.md`에 기록했고, 자동 테스트 29개와 브라우저 확인(편집·XLSX 다운로드·A4 6페이지 PDF·한글 폰트)을 통과했다. 3시간 안에 끝났으며, 확장 지점은 아래와 같다.

- 데모에는 서버 저장이 없어 `/quotes`·`/settings`·버전 이력 화면을 만들지 않았다. 대신 현재 문서를 localStorage에 두고 인쇄 화면이 이를 읽는다.
- CSV 오류는 자동으로 일부만 반영하지 않는다. 사용자가 오류 행을 제외하고 가져오기를 명시적으로 눌러야 한다.
- 조정 금액은 설명 1개 + 금액 1개 구조다. 여러 조정 항목이 필요하면 B 단계 관찰 후 목록으로 확장한다.

### B. 실제 작업 적합성 확인

- [ ] 이 작업을 하는 운영자가 제공한 익명 품목표 하나를 정해진 양식으로 변환.
- [ ] 기존 기본 출력과 새 파일을 비교해 실제 전달/수정 작업에 쓰이는지 확인.
- [ ] 필요한 열·양식·Excel 재계산 여부와 작업시간을 기록.
- [ ] 맞춤 양식 요구가 매번 달라지는지 확인하고 지원 가능한 양식 하나를 선택.

**후속 조건:** 실제 파일을 제공하고 결과를 업무에 쓰는 관측은 작은 연동 작업의 근거가 될 수 있다. 유료 수요는 별도 결제 시험으로 확인한다. 첫 데모에 반응이 없더라도 노출을 확인하지 못했다면 수요 기각으로 쓰지 않는다.

### C. 연동 MVP

- [x] OAuth·tenant DB·상품 검색/선택 구현. (토큰 DB·상품 검색/선택 완료, 견적 문서 저장은 미구현)
- [ ] 초안 저장·확정 snapshot·복제·버전 충돌 처리.
- [ ] 두 출력 adapter를 저장된 snapshot에 연결.
- [ ] 권한·파일 생성·장문/다페이지·한글 출력 테스트.
- [ ] 한 테스트몰에서 수동 설치/재접속/상품 선택/출력 검증.

**완료 조건:** 한 몰의 실제 상품을 운영자가 확인한 단가로 내보내고, 다른 몰 접근 차단과 확정 문서 보존을 검증한다. 시간·현금 상한은 이 단계 시작 전에 별도로 기록한다.

### D. 제한 파일럿과 출시

- [ ] 파일럿 몰·기간·지원 범위·지원시간 상한을 먼저 정한다.
- [ ] 견적 작성/수정 시간, 실제 사용 건수, 출력 오류, 지원시간 측정.
- [ ] 월 구독 또는 일회성 양식/설정 서비스 중 반복 사용 빈도에 맞는 오퍼 시험.
- [ ] 요금·게이트·결제/해지 정책은 유료 시험 전에 확정.
- [ ] QA.md, 앱 전용 이미지·스크린샷·지원 안내·개인정보 안내 준비.
- [ ] 등록 후 상세페이지/데모 조회와 요청·결제를 분리 계측.

## 10. 검증 사례

- 계산: 10×5,000 + 20×3,000 = 110,000원; 조정 금액 +3,000이면 113,000원. 화면/XLSX/PDF 동일.
- 입력: 빈 상품명, 0/음수 수량, 잘못된 단가, 허용 범위 초과, CSV 헤더 누락·쉼표·개행·BOM.
- 출력: 한글 폰트, 긴 이름/옵션, 여러 페이지, 열 너비, 숫자 셀, 문서 번호·버전, 품목 코드 앞자리 0.
- 보안: 수식처럼 보이는 이름이 문자열로 남는가; tenant 간 조회·수정·내보내기가 모두 차단되는가.
- 버전: 상품 가격·공급자 정보 변경 후에도 확정한 과거 문서가 동일한가; 동시 편집 충돌을 알리는가.
- 실패: 저장/파일 생성 오류 시 내용이 보존되고 재시도 가능한가; 부분 파일을 정상 다운로드로 표시하지 않는가.

데모에서는 계산 함수와 출력 파일의 핵심 결과를 가볍게 검증한다. 연동 후에는 tenant·OAuth·snapshot 보존에 자동 테스트를 추가한다. 구현 시작 시 `QA.md`에 수동 시나리오·기대값·설치 한도 소모 여부를 적고, 실제 설치/동의/재접속은 사용자가 확인하는 Cafe24 작업 절차를 따른다.

## 11. 다음 작업과 미결정 사항

**다음 구현 작업은 A의 CSV→XLSX/인쇄 PDF 데모다.** 시작할 때 llm-wiki의 작은 실험 카드에 실제 시작일·종료시각·3시간 상한·담당을 기록한다. A는 2026-09-16에 구현·검증·배포까지 마쳤고, 구현 기록과 검증 결과는 `README.md`·`QA.md`에 있다.

| 항목 | 현재 상태 |
|---|---|
| 최초 양식 | 한국어·KRW·고정 1개, 합성 10품목 |
| PDF | 브라우저 인쇄 저장부터 시작; 직접 생성은 후속 |
| Excel 재계산 | 초기 숫자 snapshot, 수식 지원 필요성 확인 |
| 세금 분리 계산 | 초기 제외, 조건 표기만; 실제 필요에 따라 별도 설계 |
| 라이브 상품 API / 기본 견적 import | 구현 전에 공식 문서·테스트몰 확인 |
| 배포·DB·XLSX 라이브러리 | 구현 시작 시 환경·비용·라이선스 확인 |
| 가격·사업 게이트 | 미확정; 지속 투자/유료 시험 전에 기록 |
| 유통·수요 | 작업명 중심 앱스토어 노출 가설, 우리 오퍼 조회·결제 미측정 |
